# ═══════════════════════════════════════════════════
# BErozgar — Production Environment Template
# ═══════════════════════════════════════════════════
#
# Copy to .env.production and fill in ALL values.
# Used by: docker compose -f docker-compose.prod.yml --env-file .env.production up -d
#
# WARNING: Never commit .env.production to version control.

# ─── Database ──────────────────────────────────────
POSTGRES_USER=berozgar
POSTGRES_PASSWORD=           # REQUIRED — strong random password
POSTGRES_DB=berozgar

# ─── JWT ───────────────────────────────────────────
JWT_SECRET=                  # REQUIRED — min 32 chars, e.g. openssl rand -base64 48
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_DAYS=7

# ─── Google OAuth ──────────────────────────────────
GOOGLE_CLIENT_ID=            # REQUIRED — from Google Cloud Console

# ─── Domain / CORS ────────────────────────────────
CORS_ORIGIN=https://berozgar.in
COOKIE_DOMAIN=.berozgar.in

# ─── Email Delivery ───────────────────────────────
# Provider: "resend" (recommended) | "smtp" | "log" (dev only)
EMAIL_PROVIDER=resend
EMAIL_FROM=BErozgar <noreply@berozgar.in>

# Resend (if EMAIL_PROVIDER=resend)
RESEND_API_KEY=              # re_xxxxx from resend.com

# SMTP fallback (if EMAIL_PROVIDER=smtp)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=

# ─── Rate Limiting ────────────────────────────────
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# ─── Monitoring (optional) ────────────────────────
SENTRY_DSN=
