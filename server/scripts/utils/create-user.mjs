import {PrismaClient} from '@prisma/client'; import bcrypt from 'bcryptjs'; const prisma = new PrismaClient(); async function main() { const pass = await bcrypt.hash('password123', 10); await prisma.user.upsert({ where: { email: 'seller123@rgit.edu' }, update: { password: pass, verified: true, role: 'STUDENT_VERIFIED', trustStatus: 'GOOD_STANDING' }, create: { email: 'seller123@rgit.edu', password: pass, fullName: 'Seller Test', verified: true, role: 'STUDENT_VERIFIED' } }); console.log('Created!'); process.exit(0); } main();
